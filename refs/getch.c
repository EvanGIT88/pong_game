// Example for getch() in C

#include <stdio.h>
#include <stdlib.h>

// Library where getch() is stored
#include <conio.h>

int main()
{
    int* getch_val = malloc(sizeof(int));
    while (*getch_val != 35) {
        
    }
    return 0;
}